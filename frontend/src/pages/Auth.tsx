import {Eye,EyeOff,Sparkles} from 'lucide-react'
import {useState} from 'react'
import {Link,useNavigate} from 'react-router-dom'
import {useAuth} from '../context/AuthContext'
export default function Auth(){const[mode,setMode]=useState<'login'|'register'>('login');const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState(false);const{login,register}=useAuth();const nav=useNavigate()
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const user=mode==='login'?await login(email,password):await register(name,email,password);nav(user.role==='admin'?'/admin':'/')}catch(err){setError(err instanceof Error?err.message:'Unable to continue')}finally{setBusy(false)}}
 return <section className="auth-page boutique-auth"><div className="auth-side"><Link className="brand" to="/"><span><Sparkles/></span>Aurelia <em>Boutique</em></Link><div><span className="eyebrow">Your private boutique account</span><h1>Save your bag, track every dress and manage your orders beautifully.</h1><p>Access size-aware checkout, order tracking and a curated fashion experience from one secure account.</p></div></div><form className="auth-form" onSubmit={submit}><div><span className="eyebrow">{mode==='login'?'Welcome back':'Join the boutique'}</span><h2>{mode==='login'?'Sign in to Aurelia':'Create your Aurelia account'}</h2><p>{mode==='login'?'Access your selected styles and orders.':'Begin your personalised boutique experience.'}</p></div>{mode==='register'&&<label>Full name<input value={name} onChange={e=>setName(e.target.value)} required/></label>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<div className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="form-error">{error}</div>}<button className="btn full" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><p className="switch-auth">{mode==='login'?'New to Aurelia?':'Already have an account?'} <button type="button" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Create account':'Sign in'}</button></p><div className="demo-note"><b>Admin demo</b><span>admin@aureliaboutique.com / ChangeMe123!</span></div></form></section>
}
